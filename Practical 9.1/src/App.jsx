import React from 'react';

const App = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      <h1>Fullstack 9.1 - Dockerized React App</h1>
      <p>Production-ready Docker image using multi-stage build.</p>
    </div>
  );
};

export default App;
